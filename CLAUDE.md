# CLAUDE.md

**Fresh session: the full project context & handoff is in [`soul.md`](soul.md). Read it first, then continue.**

@soul.md

---

TL;DR — pixel-parity macOS Tahoe "Liquid Glass" for KDE Plasma. Source of truth is
`tokens/tahoe.json`. On **Kubuntu 26 / Plasma 6**: `sudo apt install qt6-style-kvantum`,
then `./install.sh --glass-deps` (builds + enables the KWin 6 refraction effect),
then the dock → toolbar → Settings **parity loop** against `corpus/screenshots/`.
Standing rule: *when in doubt, the Apple HIG and Tahoe screenshots are the corpus.*
